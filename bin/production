#!/usr/bin/php
<?php

require __DIR__ . '/libs/bootstrap.php';

if (!existScript('composer')) {
	error('Composer command not exists.');
}

chdir(__DIR__);
command('Minify css/js:');
system('./minify');
command('Composer dump-autoload:');
chdir(__DIR__ . '/..');
system('composer dump-autoload -o');
